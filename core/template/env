# 重要说明
# 1. 配置文件需要管理员手动复制到根目录， 同时并重命名为.env
# 2. 修改的内容包括： 数据库密码(DATA_ROOTFS, MYSQL_PASSWORD, MYSQL_ROOT_PASSWORD)

# 系统名称， 可用于数据库账户名称，数据库名称等
SYSNAME=websys4

# 数据库容器名称
CONTAINER_MYSQL_NAME=mysql
# 网页服务器容器的配置
CONTAINER_WEB_NAME=web4
MYSQL_IP=x.x.x.x


# 存放数据的宿主机目录，绝对路径，最后不带/， 比如/data
ROOTFS_DATA=x
# 数据库参数配置
MYSQL_ROOT_PASSWORD=12345678
